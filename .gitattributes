# These run inside Linux containers -- CRLF line endings would break the
# shebang and shell parsing if a Windows checkout ever normalized them.
docker/entrypoint.sh text eol=lf
docker/Dockerfile text eol=lf
