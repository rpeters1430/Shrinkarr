{
  "name": "@shrinkarr/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "shrinkarr": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/static": "^10.0.0",
    "commander": "^15.0.0",
    "fast-glob": "^3.3.2",
    "fastify": "^4.28.1",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
